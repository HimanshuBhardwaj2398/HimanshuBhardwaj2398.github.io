source "https://rubygems.org"
gem "jekyll-theme-lanyon" # Matches the theme in _config.yml
gem "jekyll-feed"         # Plugin from _config.yml
gem "jekyll-seo-tag"      # Plugin from _config.yml
# Add other plugins from _config.yml here if they are gems
